# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Ignore `.github/CLAUDE.md`** — it belongs to an unrelated project ("caveman", unpacked from `.github.rar` by accident), same for `.github/prompts/` and `.github/CONTRIBUTING.md`. This file is the only valid CLAUDE.md.

## What this is

Personal website at https://bakan7.netlify.app — a Vite + React SPA with a Netlify Functions backend, all in one repo. Two products:

1. **University pages** — subject info & reviews ("tárgy info"), university links, and file sharing backed by Google Drive.
2. **Állásfigyelő (job watcher)** — ~30 scheduled scrapers ingest Hungarian student/entry-level job postings into Postgres; UI at `/allasfigyelo`. This is the bulk of the codebase.

UI text, code comments, and docs are largely Hungarian. There is no test framework; `test_jobs.js`, `script.js`, `src/temp.jsx` are scratch files and `src/Express.js` is empty.

## Commands

- `npm run dev` — Vite frontend only (calls to `/.netlify/functions/*` will 404)
- `npx netlify dev` — frontend + functions locally (needs env vars; scheduled functions still only fire when invoked manually)
- `npm run build` — production build to `dist/`
- `npm run lint` — ESLint
- `npm run server:dev` — legacy standalone Express reviews API (`server/server.js`); production uses Netlify Functions instead

Deploy = push to `main`; Netlify builds per `netlify.toml` (publish `dist`, functions from `netlify/functions`, esbuild, Node 20). `dist/` and `.netlify/functions-serve/` are build artifacts — never edit them.

## Where data lives

**Neon Postgres** — connection string in `NETLIFY_DATABASE_URL` (`NETLIFY_DATABASE_URL_UNPOOLED` for backups). There is no migrations directory: schema is created/patched lazily in function code (`CREATE TABLE IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS`).

| Table | What it holds | Writers → readers |
|---|---|---|
| `job_posts` | every scraped job posting; **`url` is the row identity**; `active` flag = still listed on source | all `cron_jobs_*` scrapers → `jobs.js` API |
| `job_categories` | category → keyword lists for classification | edited via `categories.js`, read by `load_categories.mjs` (5-min cache) |
| `targy_kerelem` | subject requests submitted from the site | `subject-request.js` |
| `bug_reports` | bug-report widget submissions | `bug-report.js` |
| `admin_applied_jobs` | jobs the admin marked as applied | `job-applied.js` (gated by `ADMIN_VISITOR_IDS` allowlist) |

`subject_reviews` moved to a Netlify Blob 2026-09-08 — see below. The old Postgres table itself was **not** dropped (left as-is, unread/unwritten, same caution as the job-stats/ats-state migrations before it — drop it in a follow-up once the blob has run live for a while).

`job_filters` moved to a Netlify Blob 2026-09-09 — see below (same caution: old Postgres table left in place, unread/unwritten from here on).

**Netlify Blobs** stores:
- `job-stats` — daily/category job-posting aggregates, single key `latest.json` (`{generatedAt, dailyStats[], dailyCategories[], dailyLanguages[], dailyTechnologies[]}`). Replaced the `job_daily_stats`/`job_daily_categories` Postgres tables 2026-09-04 (derived, nightly-rebuildable data, no relational access pattern — see `_daily_stats_store.mjs`). Written by `cron_daily_stats.mjs` (23:45 UTC daily append) and `_stats_rebuild_core.mjs` (full/range rebuild, still runnable from a disposable `tmp-*` endpoint); read by `job-stats.js` (now public — see below) and, cross-site, by pestidev.hu's `/stats` page (`app/lib/mywebsiteStats.server.ts` there, using `@netlify/blobs`' manual mode with this site's `siteID` + a Netlify PAT — **reuses `CRON_SECRET`'s value**, which is already a real PAT per the `last-deploy.js` note below). The two Postgres tables were dropped the same day once that cross-site read was confirmed live. **2026-09-16 (user request): `dailyLanguages`/`dailyTechnologies` added**, same mechanism as `dailyCategories` end to end — `_stats_core.mjs`'s new `technologyBreakdown()` splits each day's `job_posts.technologies` (a comma list of `extractTechnologies()` canonical labels) into spoken-language rows (`{date, language, count}`) vs. actual-technology rows (`{date, technology, count}`) using a new `LANGUAGE_LABELS` set in `_tech_keywords.js`; the split only feeds stats, `job_posts.technologies` itself is untouched and still mixes both (see that file's 2026-09-08 note). No intern-prefixed bucket for these two (unlike categories) — pestidev.hu shows exactly one aggregate pie each, summed across the whole history, not a month/half-year/intern breakdown. Rows before this date have no `language`/`technology` entries until a rebuild (`_stats_rebuild_core.mjs`, which now also carries `technologies` through `fetchLiveRows`/the archive merge) backfills them. **Backfilled 2026-09-16** via a disposable `tmp-langtech-backfill-background.mjs` endpoint (full-history `rebuildStats()` call, no date range) — confirmed live: `dailyLanguages` now spans 2026-03-18..2026-09-16 (209 rows), `dailyTechnologies` spans 2026-03-09..2026-09-16 (9140 rows). Gotcha hit along the way: the first attempt used the "ack 202, keep working unawaited" background-function shape and silently never ran (the exact bug class in the deploy-workflow memory) — fixed to the `export default withTimeout("name-background", handler)` shape every real `cron_jobs_*-background.mjs` uses. A second false alarm followed: after redeploying the fix, the endpoint still looked stuck for ~15+ minutes, which looked like the job being too large — it wasn't (only 3163 `job_posts` rows / 11 archive blobs total; the real rebuild took seconds). The actual cause was polling a `-background.mjs` URL to check deploy-liveness: Netlify's platform-level auto-202 for `-background`-suffixed functions fires regardless of which deployed code version is running, so "got a 202" doesn't prove the new deploy propagated — only polling a plain (non-background) function for its real body proved it. Endpoint removed after use per the usual disposable-`tmp-*` workflow. **2026-09-17 (user request): Hungarian ad-body-language detection added, closing the asymmetry the 09-16 backfill above baked in.** `extractTechnologies()` (`_experience_core.mjs`) already had `looksLikeEnglishAd()` — same word-count-threshold technique — but nothing analogous for Hungarian, so the 09-16 backfill only ever pulled "Hungarian" from the pre-existing literal `hungarian`/`magyar` `TECH_KEYWORDS` entries (an explicit language-requirement mention), never from the ad simply being written in Hungarian — undercounting the majority-language bucket on a site whose postings are almost all Hungarian. New `looksLikeHungarianAd()`, same file, wired in next to the English check; threshold tuned against representative synthetic ad text (no live DB corpus reachable from that session — see `looksLikeEnglishAd`'s own comment for how ITS threshold was tuned live, 2026-09-15). Since `job_posts` stores no raw description text, existing rows needed a live re-fetch to correct, not a SQL backfill: a disposable `tmp-hungarian-tech-backfill.mjs` re-fetched every currently-**active** row (2226) and merged in "Hungarian" (762 rows also picked up a still-missing "English" opportunistically, from before 09-15 or from sources this pass could reach that a prior pass couldn't) — 458 Hungarian additions, candidates dropping from 2071 to 1598. Inactive/archived rows are NOT covered (mostly dead links, not worth re-fetching, and nothing live reads them back except the stats rebuild, which just sums whatever `technologies` each row already has — see the `job-posts-archive` entry above). **Left incomplete, not a bug:** `profession-intern` (236 candidates) got 0 successes across two full attempts — profession.hu was returning `503` site-wide throughout (reproduced from a completely different network with the same result, so not a Netlify-IP block, more likely a real outage or a very aggressive WAF), so re-run the backfill pattern later if that source's rows still matter; `LinkedIn` (562 remaining) is expected to stay largely unfixable this way (its detail pages are effectively unfetchable anonymously) and `AI-scraped`/`wherewework`/`talent`/`alllocaljobs`/`ats-crawl` each still have a smaller unresolved tail from ordinary fetch failures (dead links, timeouts). Stats blob rebuilt afterward (`tmp-hungarian-stats-rebuild.mjs`, same `rebuildStats()` mechanism as 09-16) so `dailyLanguages`/`dailyTechnologies` reflect the corrected rows; both disposable endpoints removed per the usual workflow. **Reminder for later:** `job-stats.js` is still the broken v1 function named above — verifying the rebuild landed had to go around it (direct blob read) rather than through it. **2026-09-18 (user request): a category dimension was added to `dailyLanguages`/`dailyTechnologies`, for a per-category dropdown on pestidev.hu's Language requirements / Technologies panels** (previously a single all-categories aggregate each, no way to see "just the Data / AI postings' languages"). Row shape did not change name, just gained an optional field: a row with no `category` key is the pre-existing all-categories aggregate (unchanged, so nothing needed migrating for that bucket); a row with `category` set is the same `{date, language|technology, count}` scoped to one `job_categories` name, one row per label a job in that category carries. Written by `_stats_core.mjs`'s new `technologyBreakdownByCategory()` (groups a day's jobs by `categorize()` — the same 1-1 title→category mapping `categorizeJobs()` uses — then runs the existing per-row `technologyBreakdown()` on each group), wired into both `cron_daily_stats.mjs`'s daily append and `_stats_rebuild_core.mjs`'s `writeDays()`. On the read side, pestidev.hu's `query.server.ts` `sumLabels()` had to change to skip rows carrying a `category` (previously it summed the whole array unconditionally, which would have started double-counting the moment category-tagged rows appeared) — see that repo's own CLAUDE-equivalent notes / git history for the paired change. **Backfilled 2026-09-19 (GitHub issue #28):** the 2026-09-18 session that added `technologyBreakdownByCategory()` had committed it to a branch (`claude/category-selection-dropdown-62281d`, commit `94cfac2`) but never merged it to `main` — so it wasn't even deployed yet, on top of not being backfilled; both gaps closed in the same pass. Fast-forward merged to `main` (`94cfac2`), then a disposable `tmp-langcat-rebuild.mjs` (same shape as `tmp-hungarian-stats-rebuild.mjs`, synchronous not `-background`, plus a `readCategoryCoverage` diagnostic action mirroring the 09-17 `readLangTotals` one) ran a full-history `rebuildStats()` — confirmed live via direct blob read: `dailyLanguages` 1210 rows (938 category-tagged, spanning 2026-03-18..2026-09-19, all 15 `job_categories` names present), `dailyTechnologies` 25960 rows (16372 category-tagged, spanning 2026-03-09..2026-09-19). Endpoint removed after verification per the usual workflow.
- `recovery-logs` — self-healing events (URL migrations, reactivations), written by `_error-logger.mjs` (the companion `fetch-error-logs` store and its `clear-error-blobs.mjs` cleanup endpoint were removed 2026-09-01 — scraper fetch failures now go to the Netlify function log only)
- `weekly-backups` — `subject_reviews` JSON dumps (despite the name, runs monthly on the 1st, ~10-day retention — `weekly-backup.js`). Source switched from Postgres to the `subject-reviews` blob 2026-09-08 (see below) — this is now a disaster-recovery snapshot of that blob (bad write/bug/bad deploy), not a Postgres backup; a blob always being "current" doesn't remove the need for a point-in-time copy of it.
- `ai-scraped-registry` — the AI discovery routine's cross-run memory (checked sites + `lastChecked`, permanently-rejected list, already-found URLs), read/written by `ai-registry.mjs`
- `zip-jobs` — async Drive-folder ZIP job results (`download-folder-background.mjs` → `download-result.mjs`)
- `job-posts-archive` — cold storage for `job_posts` rows that have been `active=false` for 60+ days (past both the frontend's 30-day inactive-visibility window and `reviveSweepDead`'s 45-day self-healing window, so nothing live reads them back); one JSON blob per month, written and then deleted from Postgres by `cron_jobposts_cleanup.mjs` — added 2026-08-18 as the table's inactive-row backlog started growing unbounded
- `job-events` — upcoming job-fair / company-event listings (added 2026-09-08), single key `latest.json` (`{generatedAt, events[]}`, each event `{url, title, date, endDate, time, endTime, location, company, type, format, language, free, registrationDeadline, deadlineChecked, source, firstSeenAt}`). `type` ∈ `allasborze|eloadas|konferencia|meetup|egyeb` and `registrationDeadline` (ISO date, high-priority field per user request) were added 2026-09-08, widening scope past job fairs to talks/presentations/webinars/conferences/meetups. **2026-09-09 (user request):** the extraction scope widened again — no longer "aimed at students/job seekers" only, now any broad-interest IT/tech event counts (narrowed only against paid exec-only training and pure vendor pitches) — and two new fields, `format` (`online|inperson|hybrid`) and `language` (`hu|en`), were added as filter dimensions for the frontend; see `_ai_events_extract_core.mjs`'s prompt for the exact rules (both are required-with-fallback, like `type` — never left ambiguous-null). Written every 2 days (05:30 UTC on odd calendar days — was weekly for about a day, daily before that until 2026-09-11; each change was a user decision, the event list changes slowly enough that daily was needless load/cost) by `cron_job_events-background.mjs`, from the listing pages registered in the `event_sources` Postgres table (managed via `event-sources.js`; seeded 2026-09-08 with `epam` and `progmasters`). **2026-09-11 (user request — "no AI, only sources that are simple or have an API"):** `event_sources.mode` now picks a per-source extractor — `jsonld` runs `_events_jsonld_core.mjs`, a deterministic parser for a listing page's own `schema.org/Event` JSON-LD (cheerio + `JSON.parse`, zero LLM calls, immune to the AI-hallucination bug class entirely since there's no model in that path to invent a URL); `llm-read` is the original AI path (`_ai_events_extract_core.mjs`). `kibernaptar` (`kibernaptar.hu/esemenylista/`) was checked live and does embed full per-event JSON-LD (WordPress events-calendar plugin) — switched to `jsonld`. `ivsz.hu` and `mvisz.hu` (added 2026-09-09/10 as general listing sources) were checked too and do **not** — no structured event data, just generic Yoast SEO boilerplate — so per the "only simple" instruction both were **removed from `event_sources` entirely** rather than built as fragile HTML-pattern scrapers. `epam`/`progmasters` stay on `llm-read` (2 small single-event company pages, no structured data, but trivial cost). The `jsonld` path has real accuracy caveats documented in its own file header — `eventAttendanceMode` on kibernaptar is always "Offline" regardless of the event's actual format (a plugin default nobody configures), so `format` is inferred from `location.name` containing "online" instead (catches explicit online events, under-detects hybrid — 2 events that were hand-corrected to `hybrid`/`online` on 2026-09-09 will revert to the classifier's `inperson` guess once this path re-touches them); `type` is keyword-classified from the title (same pattern as `job_categories`); `registrationDeadline` is always null (no per-event AI follow-up in this path — that follow-up is itself an AI call, dropping it is the actual "no AI" trade). Merged/purged via `_job_events_store.mjs`, whose `mergeAndPurgeEvents` drops any event whose end (or start) date is before today on **every** run, source or no source (mostly field-agnostic — every field beyond `url`/`date`/`endDate`/`free` just passes through; `free` is the one other field it actively inspects, see the 2026-09-22 note below). Read by `job-events.mjs` (public, same reasoning as `job-stats.js`; was a v1 `job-events.js` that 500'd on every read until fixed 2026-09-09, see the Functions v1/v2 gotcha below) and shown as a small panel in `JobWatcher.jsx` (title/date/location/registration-deadline — not updated for `format`/`language`, out of scope for an admin-only glance panel). **The pestidev.hu side already reads this blob cross-site** (`app/lib/mywebsiteEvents.server.ts` + `app/routes/events.tsx` there, same siteID+PAT pattern as `job-stats`) — discovered live and working 2026-09-09 while investigating a "why don't I see deadlines" report (turned out none of the events at the time actually had one — not a bug). **2026-09-10:** that repo's `/events` page was rebuilt from a plain list into a month-calendar view with type/format/language filter chips (`app/components/ui/EventsCalendar.tsx`, PR #32 there) — iterated with the user via a throwaway Artifact comparing layout concepts first; design/implementation work for that page belongs in that repo, not here. **2026-09-22 (user request — the board is meant to be free-only, and paid conferences/trainings had visibly started appearing after the 2026-09-09 scope widening; separately, a date with no time is close to useless for adding an event to a calendar):** two more fields, `free` (boolean) and `time`/`endTime` (`"HH:MM"` 24h, null if the source states none), added end to end in both extractors. `llm-read`'s `free` is required-with-fallback in the schema (same defensiveness as `type`/`format`/`language`) with an explicit prompt rule: a stated ticket price/purchase requirement → false, no pricing info at all → default true. `jsonld`'s `classifyFree()` derives it deterministically from kibernaptar's own schema.org `offers.price` (0 → free, any other numeric price → paid) but defaults an ABSENT/non-numeric price to **not** free — spot-checked live against two real events with no `offers` block at all (ITBN CONF-EXPO, "11. Cybersec konferencia") that are both genuinely paid, so "no price given" reads as a missing paid-tier here, not a free walk-in. `time`/`endTime` on the `jsonld` path come from the same `startDate`/`endDate` ISO datetimes `dateOnly()` was already truncating away; exactly `"00:00"` is treated as "no time stated" (the plugin's own default for an unset start time, same caveat class as `eventAttendanceMode`). The actual removal of paid rows lives in `_job_events_store.mjs`'s `mergeAndPurgeEvents` (`free === false` dropped alongside past-dated rows, from BOTH incoming and previously-stored rows) — see that file's header. A one-time disposable cleanup (`tmp-events-free-cleanup-background.mjs` + `-result.mjs`, deploy→invoke→delete workflow) reclassified every event stored before this fix (no `free` field at all yet) via a per-event detail-page fetch + a cheap haiku single-field lookup (`classifyEventPricing` in `_ai_events_extract_core.mjs`), defaulting a failed fetch to excluded. The "Event source discovery (Hungarian IT events)" weekly cloud routine (`trig_01XLinMxgkBpnGtXCHKdh81E`, Wednesdays 03:00 UTC — registers new `event_sources` rows, does not extract events itself) had its prompt updated the same day to reject a candidate source whose events are entirely/predominantly paid-ticketed, so it stops feeding sources that would just get filtered out downstream anyway. **Same-day incident:** a cleanup pass to backfill `free` on pre-existing rows wiped the blob to 0 because `ANTHROPIC_API_KEY` turned out to be missing from the Netlify production environment (every classification call failed and hit the "unknown → exclude" default across all 61 stored rows) — recovered the 6 kibernaptar rows recoverable without that key; the other ~55 rows turned out to have come from an undocumented one-off load that predates any of `event_sources`'s 3 rows at the time (confirmed via `RemoteTrigger list_runs` — the discovery routine had never actually fired), not the registered-source pipeline, so they weren't recoverable by re-running it — one new source (`njszt.hu`, confirmed-free professional-society events) was found and registered in their place after a fresh manual source search. **Also 2026-09-22, same day, pestidev.hu issue #56:** the same undocumented load had also pulled in foreign paid conferences with no Hungary connection at all (STARWEST/Anaheim, Targeting Quality/Ontario, WeTest/Thessaloniki, Tesena Fest/Prague, CTAP/Groningen) — `_job_events_store.mjs` gained a second store-level filter alongside `free`, originally `looksHungaryRelevant()` (any Hungarian city). **User corrected the same day** ("de személyesen csak budapest") — the site's own tagline is "Csak Budapest · Csak IT", so a vidéki Hungarian venue is equally out of scope for an in-person event, not just a foreign one. Renamed to `looksBudapestRelevant()`: `inperson`-format events are dropped unless `location` names Budapest specifically (this dropped 2 kibernaptar events, Szombathely/Veszprém, the same day it shipped); `online`/`hybrid` events pass regardless of venue, since they're attendable from Budapest regardless of where they're physically hosted. Missing `location` is a permissive pass either way (not itself a signal of being non-Budapest).
**2026-09-22, later same day (user request — "keresd az eseményeket és töltsd fel... főleg ingyenes fejlesztői/tesztelői/AI"):** a manual WebSearch/WebFetch pass for free dev/tester/AI-focused Budapest events found meetup.com's group `/events` listing pages are a dead end for both the raw-fetch `jsonld` path (no structured data at all) AND `llm-read` in practice (checked ~8 groups — BudapestJS/budapest.py/Teszt & Tea/DevBP/Budapest AI Meetup/etc. — all show "0 events" even via WebFetch's own rendering, apparently client-side-rendered past what a non-JS fetch sees; only a meetup.com event's OWN detail page, found via search snippet or a direct link, renders real content). Confirmed leads that turned out paid/dead: `techconf.hu` (real conferences, all paid, no per-event price signal on the listing page itself so registering it would mis-classify them free), `dev.events` (global aggregator, `EducationEvent` not `Event` type, several results turned out to be a paid `dev.club` membership platform, not real Hungarian meetups), `uxnaptar.hu` (JSON-LD is generic `WebSite` SEO boilerplate, not per-event), `aimeetup.hu` (real Budapest + free schema.org markup but zero scheduled events — "coming soon"), GDG Budapest DevFest (no 2026 edition announced yet, `devfest.hu` still redirects to the 2025 one), KCD Budapest / Compass AI & Tech Summit / Budapest BI & Analytics Forum (all confirmed or strongly implied paid). One real find: `rendezveny.hwsw.hu` (HWSW's event platform, distinct from the news site `hwsw.hu`) mixes free `/kraftie/` meetups with paid `/kepzes/` (training) and `/partner/` (conference) URLs on the same listing page — registered as a new `llm-read` `event_sources` row (`hwsw`), and its one confirmed-free upcoming event (kraftie roundtable "Tervezd újra a karriered az agentek világában", 2026-10-21, 17:00-19:00, hybrid Budapest, free, reg. deadline 2026-10-16) was manually inserted via a disposable endpoint since `ANTHROPIC_API_KEY` is still missing so the automated `llm-read` path can't run it itself yet — once that key is restored, the regular cron will pick this and future events off that page on its own.
  - **Note (2026-09-08):** a parallel session initially planned a second, independent implementation of this same feature (external Claude Code routine + open web search + a separate `career-events` blob/token, no curated `event_sources` gate) before discovering this one already existed and was live — abandoned in favor of extending this pipeline instead. If a future session proposes a fresh "event calendar" build from scratch, check here first.
- `ats-state` — ATS crawl/discover bookkeeping (`tenants.json`, `candidates.json`), moved off Postgres (`ats_tenants` / `ats_slug_candidates` / `ats_seen_companies`) 2026-09-03: pure crawl-state churn nothing else ever read, but generating thousands of small writes/day (`seedTenants` alone re-asserted ~35 no-op INSERTs every hourly run). Read once, written once per run (see `_ats_state.mjs` header) by `cron_jobs_ATSCRAWL-background.mjs`, `cron_ats_discover-background.mjs`, `ats-tenants.mjs`, and `_ats_handoff.mjs`. `job_posts` itself, and the intake anti-join against `job_posts.company`, stayed in Postgres. The three old Postgres tables were dropped 2026-09-04 (via a disposable `tmp-drop-ats-tables.mjs`, after confirming row counts matched the migrated blob: 335 tenants, 2473 candidates, 1415 seen companies)
- `subject-reviews` — the LIVE `subject_reviews` data (moved off Postgres 2026-09-08, user decision), single key `reviews.json` (`{nextId, reviews: [{id, name, user, difficulty, usefulness, general, duringSemester, exam, year, semester, user_id, kepzes_fajtaja, likes}]}`). **Different risk profile from every other blob migration above**: those (`job-stats`, `ats-state`) are derived/rebuildable data with a single or cron-only writer, so a plain whole-blob read-modify-write was safe by assumption ("no concurrent writer"). `subject_reviews` is hand-written user content with public, concurrent writers — any visitor can create/edit/delete a review, and the new like feature (a heart icon per review, one like per browser via the same `localStorage.userId` already used for edit/delete ownership) means any visitor can write on every page view. So `_subject_reviews_store.mjs` does NOT use that pattern: every write goes through `mutateReviews`, an optimistic-concurrency retry loop built on `@netlify/blobs`' `getWithMetadata`/`setJSON({onlyIfMatch: etag})` primitive (confirmed in `node_modules/@netlify/blobs/dist/main.js`) — read+etag, mutate in memory, write conditioned on that etag, retry from a fresh read on conflict. This is the guarantee a Postgres row UPDATE gives for free; **any future migration of a table with public/concurrent writers needs the same pattern, not the "no concurrent writer" one.** `likes` (liker `user_id`s) is never sent to clients raw — GET responses carry only the derived `likeCount`/`likedByMe` (see `toPublicRow`), so one visitor's id can't be read off another's like list. Read/written by `reviews.mjs` (all CRUD + the new `POST /reviews/:id/like` toggle route — see the Functions v1/v2 gotcha under "Backend" for why this is `.mjs` and not `.js`); the monthly `weekly-backups` snapshot (below) now sources from this blob instead of Postgres. `seed-subjects.mjs` (root) still targets the old Postgres table and is stale/unused for future seeding.
- `job-filters` — the title-word denylist (formerly `job_filters` Postgres table), moved 2026-09-09. Single key `filters.json` (`{nextId, words: [{id, word}]}`). Back in the "no concurrent writer" bucket like `job-stats`/`ats-state`, not `subject-reviews`'s etag/retry pattern — writes are admin-only (`ADMIN_SECRET`-gated CRUD in `filters.mjs`, one operator editing keywords, not public traffic), so `_job_filters_store.mjs` does a plain whole-blob read-modify-write. Read by `load_filters.mjs` (5-min in-memory cache, unchanged from the Postgres version) — the actual consumer, imported by every scraper's insert-time denylist check — and by `filters.mjs` (CRUD; its PATCH action for counting/purging matching `job_posts` rows stays on Postgres, since that operates on `job_posts` not `job_filters`) and `audit-data.mjs`'s `?filters=1` route. Migrated via a disposable `tmp-migrate-job-filters.mjs` (id/word 1:1 copy, `nextId` set past the old table's max id); old Postgres table left in place, unread/unwritten, same caution as the migrations above — drop it in a follow-up once this has run live a while.
- `download-counts` — per-folder download counters for the subject file browser (`SemesterPreview.jsx`, added 2026-09-10, user request). Single key `counts.json` (`{ counts: { [driveFolderId]: number } }`). Public, concurrent writers (any visitor clicking any download button), so `_download_count_store.mjs` uses the same etag/`onlyIfMatch` optimistic-concurrency retry pattern as `subject-reviews`'s likes, not the "no concurrent writer" plain-RMW pattern. Granularity is per-folder, not per-file: a subject's root Drive folder and each of its immediate (first-level) subfolders get their own counter; downloads from anything nested deeper roll up into their first-level ancestor's counter (`countKeyForStack` in `SemesterPreview.jsx`). Incremented by the folder-ZIP download button, the per-row file download icon, and the file-preview-modal download link (all three read/write the same folder-level counter and show the count inline). Read/written via `download-count.mjs` (v2 `.mjs`, GET returns the whole map, POST `{folderId}` increments).

**Google Drive** — the actual file storage for subject materials. Reads use rotating API keys (`drive-files`, `list-files-recursive`, `proxy-file`, `download-folder*`); uploads use a service account (`upload-to-drive.js`, setup in `DRIVE_UPLOAD_SETUP.md`).

**Static in-repo data** — `src/components/semesterData_pretty.js` (semester/subject structure rendered on the site).

## Architecture

### Frontend (`src/`)
React Router SPA; all routes live in `src/App.jsx` with Hungarian paths: `/targy_info`, `/egyetemi_linkek`, `/User_oldalak`, `/rolam`, `/allasfigyelo` plus hidden `/allasfigyelo/filters`, `/allasfigyelo/categories`, `/allasfigyelo/stats`. Everything calls the backend at `/.netlify/functions/...`. `JobWatcher.jsx` is the job-board UI; admin-only features unlock by browser visitor-id matching the allowlist in `job-applied.js`.

### Backend (`netlify/functions/`)
- **HTTP API**: `jobs.js` (job list; per-warm-container 60s cache; CORS pinned to the prod origin), `job-stats.js`, `job-events.mjs` (public read of the `job-events` Blob — see "Where data lives"), `event-sources.js` (AI-family-token-gated CRUD for the event-listing source registry), `categories.js`, `filters.mjs` (note the extension — see the Functions v1/v2 gotcha below), `reviews.mjs` (note the extension — see the Functions v1/v2 gotcha below), `bug-report.js`, `subject-request.js`, visitor tracking, Drive functions.

**Functions API v1 (`exports.handler`, CommonJS) does NOT get Netlify's automatic Blobs context on this site — v2 (`export default`, `.mjs`) does.** Discovered 2026-09-08 while wiring `reviews.mjs`'s (then `reviews.js`) Blob access: it 500'd with `MissingBlobsEnvironmentError` in production, and so — turns out — do the pre-existing `job-stats.js` and `job-events.js`, both classic `exports.handler` functions that read Blobs directly, neither touched by that change. Every already-working Blob-backed function in this repo (`_daily_stats_store.mjs`, `_ats_state.mjs`, `_job_events_store.mjs`, `ats-tenants.mjs`, …) is a v2 `.mjs` function; `reviews.js` was rewritten to `reviews.mjs` (v2) to fix it, confirmed live. `job-events.js` got the same fix 2026-09-09 (rewritten to `job-events.mjs`) — this was silently breaking the job-fair/event panel end to end (registration-deadline extraction and the past-event purge were both already working on the write side; nothing could ever be read back). `filters.js` was rewritten to `filters.mjs` the same day when `job_filters` moved to a Blob (see "Where data lives") — same shape, same reason, needed from the start this time since the CRUD now touches Blobs directly. **`job-stats.js` is still broken as of this writing** — same fix not yet applied there. **Any new Blob-backed HTTP endpoint MUST be a v2 (`.mjs`, `export default`, Request/Response) function**, not `exports.handler` — the old shape will silently 500 on every Blobs call. A secondary trap found along the way: don't nest a CommonJS `require("@netlify/blobs")` inside a `.js` helper that's `import`-ed from a `.mjs` entry point — esbuild bundles that as a `__require` shim Netlify's runtime can't resolve at runtime (`Cannot find module '@netlify/blobs'`), even though the direct `import` form works fine. Keep Blob-touching helper modules `.mjs` all the way down.
- **Shared cores** (prefix `_`, not routable): `_active_core.mjs` (active-flag reconcile + volatile-URL migration), `_linkedin_core.mjs`, `_experience_core.mjs` / `_profession_core.mjs` (backfill classification), `_error-logger.mjs` (`withTimeout` wrapper + blob logging), `_backup-core.js`, `_stats_core.mjs` + `_stats_rebuild_core.mjs` (see below).

**Stats are derived data — rebuild them, don't patch them.** The `job-stats` Blob (**2026-09-04, replaced the `job_daily_stats`/`job_daily_categories` Postgres tables**, since dropped — see "Where data lives") is just a nightly *snapshot* of rules that keep changing, so every category rename/retune leaves the saved history stale. **Since 2026-09-01 the classification rules are NOT owned by this repo.** The published board at pestidev.hu (separate repo `Andrssss/allasfigyelo`, local clone `c:\Users\Andris\allasfigyelo`) renders this same blob on its stats page (cross-site read, see the `job-stats` Blob entry above), so the numbers have to mean what ITS filters mean — its `app/lib/categorize.ts` + `app/lib/experience.ts` are the reference implementation. This repo carries a 1-1 port in **`src/lib/categorize.mjs`** and **`src/lib/experienceLevel.mjs`**, and there is now exactly **one** copy: both `src/JobWatcher.jsx` (admin board) and `_stats_core.mjs` (which is now only the day-level aggregation) import it. **If the rule changes there, re-port it here, then rebuild** — three independently-edited copies had silently drifted before 2026-08-26, and the port itself was overdue: the v2 keyword table uses a `~` STEM prefix (34 such keywords live in `job_categories`) that this repo's old `*`-wildcard matcher could not read at all, so ~370 postings per archive sample sat in "uncategorised" that belonged on a real shelf. Note the fallback category is `Fejlesztő` (the actual DB name) and the no-match bucket is `Egyéb`. **`netlify/functions/_experience_core.mjs` is a different thing and must NOT be aligned to this** — that is the scrapers' WRITE side (what goes into `job_posts.experience`); `src/lib/experienceLevel.mjs` is the READ side (how a stored value is classified). `_stats_rebuild_core.mjs` recomputes any date range from the raw postings — `job_posts` **plus the `job-posts-archive` blobs**, without which every day older than ~60 days rebuilds as zero — and writes each range into the `job-stats` blob (`replaceDays`, `_daily_stats_store.mjs`) in one call. Run it from a disposable `tmp-*` endpoint (blobs need site context, and the prod connection string isn't available locally); the local `backfill_daily_stats.mjs` CLI is gitignored and needs `NETLIFY_SITE_ID`/`NETLIFY_API_TOKEN` to see the archive.

### Scraper/cron system
~30 `cron_jobs_<SOURCE>-background.mjs` Netlify **background** functions ingest into `job_posts`. Scheduling is two-tier (reference doc: `netlify/functions/CRON_SCHEDULE.md`):

- **Directly scheduled** via `export const config = { schedule: "..." }` in-file: LinkedIn shards `cron_jobs_L_1..L_8` (staggered minutes, hours `4-22` UTC), `cron_experience-background.mjs`, `cron_daily_stats.mjs` (23:59), `cron_job_events-background.mjs` (every 2 days, 05:30 UTC — was weekly, then daily before that, all user decisions on 2026-09-11 — job-events, see "Where data lives"), `weekly-backup.js`. (`cron_stats_cleanup.mjs` — a monthly "archive last month to the `stats-backups` blob store, then DELETE it from both stats tables" job — was **deleted 2026-09-01**: it had no `config.schedule` and no caller, so it had never actually run, and had it ever been wired up it would have eaten the 6-month graph that pestidev.hu's stats page draws. The stats are derived data — rebuild them, don't prune them.)
- **Dispatcher-triggered**: `cron_dispatcher.mjs` (hourly, high-volume sources), `cron_dispatcher_daily.mjs` (14:00 UTC, sources with <10 postings), `cron_dispatcher_test.mjs`, and `cron_jobs_P.mjs` POST to the background workers with `Authorization: Bearer $CRON_SECRET`. Workers reject requests without the secret.

**Scraper invariants — keep these when touching any scraper:**
- Upsert keyed on `url`. Sources whose URLs contain volatile IDs must go through `migrateVolatileUrl` (`_active_core.mjs`) so the row migrates in place instead of churning (insert-new + deactivate-old breaks dedupe and stats).
- At the end of a **complete** run call `reconcileActive(client, source, foundUrls)`: reactivates re-seen rows, deactivates rows older than `ACTIVE_GRACE_DAYS` (3, by `first_seen`) that vanished. It deliberately does nothing on an empty result set or `complete:false`, so a blocked/broken crawl can never mass-deactivate a source. Also beware two scrapers reconciling the same `source` value — they wipe each other's finds.
- LinkedIn never uses the active model (it only sees a recent window); it stays time-based on the frontend.
- Wrap every cron handler in `withTimeout` so a crash or a hung run is logged (and the process killed) instead of dying silently.

**`cron_jobs_ATSCRAWL-background.mjs` also feeds the separate marketing_scraper project (2026-09-02).** It harvests each company's *entire* public ATS board (Ashby/Greenhouse/Lever/SmartRecruiters/…) and keeps only the IT-relevant rows for this repo's `job_posts` — the rest used to be silently dropped. A same-day sibling project change deleted marketing_scraper's own independent ATS crawler (it was re-fetching these exact same boards) in favor of this repo handing off the non-IT rows it already has in hand: `_marketing_match.mjs` (a ported, title-only marketing/sales/HR/admin scope gate — keep it in sync with marketing_scraper's `CATEGORIES` if that ever changes, same drift risk as the categorize.mjs port above) decides which dropped rows are in scope, and `_marketing_handoff.mjs` POSTs them to marketing_scraper's own `ai-ingest.mjs` (bearer `MARKETING_AI_INGEST_TOKEN`, `source: "ATS"`). That endpoint owns its own dedup (`ON CONFLICT (source, url) DO NOTHING`, insert-only) and its own seniority/blacklist gates — this repo does no filtering beyond the scope gate. Fail-soft: a hand-off failure only logs, never affects the IT-side crawl.

**Validating "is source X broken?" / "are there really no new postings?" — always check a different way than the scraper itself:**
Never validate a scraper's live behavior by re-running (or re-importing) that scraper's own extraction/URL-building/filter functions against a fresh fetch — that only proves the code agrees with itself. A bug in the original logic (wrong category/tag IDs, a bad senior-filter, a broken slug-builder) reproduces identically and looks like confirmation instead of getting caught. Validate through a channel that shares no code or assumptions with the scraper: the public site's own listing/search UI, a broader/unfiltered query, a different endpoint, a sitemap, or a plain visual check of the source — then diff *that* against what's in the DB. (2026-07-21: "is dreamjobs broken" was checked by copy-pasting `buildDreamJobsUrl`/`isSeniorLike` out of `cron_jobs_MIX-background.mjs` and running them against a fresh pull of the scraper's own two hardcoded API URLs — worthless as independent proof, even though the conclusion happened to be right.)

### Live audit docs — update them when you fix a scraper
- `CRON_JOBS_AUDIT.md` (root) — per-source fetch/pagination/completeness checklist
- `DEACTIVATION_AUDIT.md` (root) — per-source active-flag correctness checklist
- `scripts/*_SCRAPE_RESEARCH.md` — per-source endpoint/API research notes

## Environment variables

Set in the Netlify dashboard (local `.env` is minimal). Names in use: `NETLIFY_DATABASE_URL`, `NETLIFY_DATABASE_URL_UNPOOLED`, `CRON_SECRET`, `URL` (Netlify-provided; dispatchers build worker URLs from it), `ALLOWED_ORIGIN`, `GDRIVE_API_KEY_1`/`GDRIVE_API_KEY_2`/`GOOGLE_DRIVE_API_KEY`, `GOOGLE_SERVICE_ACCOUNT_JSON`, `DRIVE_UPLOAD_FOLDER_ID`, `MINDDIAK_API_BEARER`, `LINKEDIN_DISABLED`, `ANTHROPIC_API_KEY` (only for `cron_jobs_AI-background.mjs`, which is currently on no schedule — the discovery routine does NOT use it, see `AI_SCRAPER_PLAN.md`), `AI_SCRAPER_MODEL` (optional model override, default `claude-opus-4-8`), `AI_INGEST_TOKEN` (bearer for `ai-registry.mjs` + `ai-ingest.mjs`; scoped on purpose so the discovery routine never holds `CRON_SECRET` — falls back to `CRON_SECRET` when unset), `STARTUPJOBS_API_KEY` (bearer for `cron_jobs_STARTUPJOBS-background.mjs`'s calls to the official `api.startup.jobs/v1` REST API — free-tier key from a startup.jobs account, see that file's header), `MARKETING_AI_INGEST_TOKEN` (bearer for `_marketing_handoff.mjs`'s POSTs to the **separate** marketing_scraper project's `ai-ingest.mjs` at `megkellelnemvalahogy.netlify.app` — must be set to the *same value* as that other site's own `AI_INGEST_TOKEN`/`CRON_SECRET`, since the two are unrelated Netlify sites; see "Scraper/cron system" below for what this feeds).

`last-deploy.js` (powers the "Utoljára frissítve" timestamp on `/allasfigyelo`) reuses `CRON_SECRET` as the bearer token for the Netlify API call `GET api.netlify.com/api/v1/sites/{SITE_ID}/deploys` — that only works if `CRON_SECRET`'s value is actually set to a Netlify personal access token (User settings → Applications → New access token), not just an arbitrary app secret. `SITE_ID` is auto-provided to functions at runtime, no setup needed.

**Admin credentials — two tiers, deliberately separate.** Never hardcode either in source: an earlier model used a source-committed visitor UUID as the admin credential, which made the public repo the password.
- `ADMIN_SECRET` — **write/destructive** tier. Bearer token required for every non-GET action on `filters.mjs` and `categories.js` (adding/deleting filter words & categories, and the `job_posts` purge). Falls back to `CRON_SECRET` when unset. Sent as `Authorization: Bearer …` by `src/adminAuth.js` (`adminFetch`), which stores it in localStorage after a one-time prompt on the hidden admin pages.
- `ADMIN_1`, `ADMIN_2`, `ADMIN_3`, … — **read** tier: the device's own `jobWatcherVisitorId` cookie (a UUID), matched against these env vars. Grants the whole admin board (page access via `job-access.js`) and makes `jobs.js` return `hidden` rows plus the `hidden` column itself. Nothing to set up per device — the cookie already exists; read the UUID with the "Eszköz-azonosító" button on `/allasfigyelo/filters` and paste it into an env var. `_admin_identity_core.js` auto-discovers every env var matching `^ADMIN_\d+$`, so **adding a device is an env change only — no code edit**; the `_\d+` suffix is mandatory, so a misspelled name grants nobody access instead of silently becoming a key. One UUID per device/person means any single one can be revoked without disturbing the others.
  - **The second, read-only `LITTLE_ADMIN*` tier was REMOVED 2026-09-01** (env vars deleted, then the code: `_admin_identity_core.js`, `jobs.js`, `job-access.js`, `job-applied.js`, `JobAccessGate.jsx` and ~12 `JobWatcher.jsx` call sites). It existed to let a device see `hidden` rows without holding a write credential, but the 2026-08-25 page-wide gate had already collapsed both tiers to the same page access, so it only bought a separate applied-jobs bucket. `job-access.js` still returns `tier` as a string (`"admin"` or `null`) rather than a boolean, so a future second tier needs no reshape. Old `owner_key='little'` rows are still in `admin_applied_jobs`, simply unreachable.
  - **Known, and now higher-stakes than it was:** four of the configured UUIDs used to be hardcoded in `ADMIN_VISITOR_IDS` (`src/JobWatcher.jsx` — removed 2026-08-25) and are still hardcoded in `bug-report.js` and `daily-visitor.js`, i.e. published in this repo and its git history. Anyone can read one and set it as their `jobWatcherVisitorId` cookie. Accepting that was a deliberate 2026-07-23 owner decision back when it only bought you *hidden rows* ("decluttered off my board", not "confidential"). **Since the 2026-08-25 page-wide gate below, the same cookie now buys the whole admin board** — job list, stats, filter/category lists — so the accepted blast radius grew. Writes are still safe (they need `ADMIN_SECRET`). Closing it is env-only and breaks no feature: rotate each admin to a fresh UUID, update `ADMIN_*`, and set the new UUID as each device's cookie. Note `jobs.js` folds admin-ness into its cache key and switches to `private, no-store`; keep that if you touch the caching, or an admin response will leak to ordinary visitors.

**`/allasfigyelo` is admin-only (2026-08-25).** Ordinary visitors get redirected to `https://pestidev.hu` — new-postings browsing lives there now; this site keeps only the admin side. Two halves, and both are needed:
- **Client:** `src/JobAccessGate.jsx` wraps all four `/allasfigyelo*` routes in `src/App.jsx`. It asks `job-access.js` (GET → `{access, tier}`, no data, `private, no-store`) and either renders the page or `window.location.replace`s to pestidev.hu. `JobWatcher` takes both admin flags from that gate's context (`useJobAccess()`) instead of the old source-committed UUID set and the old `jobs?limit=1` `hidden`-column sniff — both are gone.
- **Server:** `hasJobBoardAccess(event)` in `_admin_identity_core.js` (= either admin tier by cookie, **or** an `ADMIN_SECRET` bearer so repo scripts and curl still work) gates `jobs.js`, `last-deploy.js`, and the previously-public GETs of `filters.mjs` and `categories.js`. The redirect is UX; this is the actual lock. `job-access.js` is the one endpoint an ordinary visitor may still call — it has to be, since only the server can match the cookie against the env vars — and it answers `200 {access:false}`, never 401, so the client can tell "you're a visitor" apart from "auth broke". **`job-stats.js` was un-gated 2026-09-04** — it never protected anything: pestidev.hu's public stats page already shows the same aggregate numbers and, since the blob migration, reads the exact same store.

**Write/destructive tier — every job- and filter-touching endpoint (2026-08-28 audit).** A sweep of every routable function that touches `job_posts` / `job_filters` / `job_categories` / `job_daily_*` / `ai_extractors` / `ats_tenants` / `admin_applied_jobs` found two with **no authentication at all**, both live in production:
- `ai-extractors.js` — CRUD over the AI-scraper site registry, answering `200` to anonymous callers. A POST here adds an arbitrary `list_url` that `cron_jobs_AI-background.mjs` then fetches on our infrastructure and ingests into `AI - <site>`; a DELETE empties the registry. Now gated on **every** method with `AI_INGEST_TOKEN` (falling back to `CRON_SECRET`) — deliberately the AI family's own narrow token, the same one `ai-ingest` / `ai-registry` / `ai-deactivate` / `ats-tenants` use, **not** `ADMIN_SECRET`: the AI paths never hold the destructive key, and this endpoint belongs to that family. Nothing in the browser calls it — it is a curl-only admin API.
- `jobPipeline.js` — dead first-generation LinkedIn scraper, referenced nowhere, but anonymously callable and happy to fetch linkedin.com with caller-supplied `keyword`/`geo`/`pages`. Gated with `ADMIN_SECRET`; it should really just be deleted.

Also moved: `fix-job-field.mjs` from `CRON_SECRET` to `ADMIN_SECRET` (falling back, as elsewhere) — it is a manual `job_posts` write that deliberately bypasses the anti-clobber guard, so it belongs in the write tier, not on the key that drives the whole cron fleet.

Deliberately left alone: the routine-scoped tokens (`AI_INGEST_TOKEN` on `ai-ingest`/`ai-registry`/`ai-deactivate`/`ats-tenants`/`ai-mcp`, `AUDIT_TOKEN` on `audit-data`/`audit-report`) stay narrow — folding them into `ADMIN_SECRET` would put the destructive credential into a cloud routine's prompt. `daily-visitor.js` is site-wide visitor analytics, not a job endpoint, and stays public (note it is also referenced by nothing in `src/`). `netlify/functions/backfill_daily_stats.mjs` has no auth and runs `main()` at module load, but it is **gitignored and therefore never deployed** — harmless today, a landmine if it is ever committed; it belongs in `scripts/`, not in the functions directory.

Scrapers are unaffected: they read `job_categories`/`job_filters` straight from the DB via `load_categories.mjs` / `load_filters.mjs`, never over HTTP. `bug-report.js` stays public (the site-wide bug widget uses it). `scripts/audit_all_sources.mjs` and `scripts/check_false_deactivations.mjs` hit `jobs.js` anonymously and now need an `Authorization: Bearer $ADMIN_SECRET` header.
